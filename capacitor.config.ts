import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.successbox.igcse',
  appName: 'IGCSE MasterPre',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
